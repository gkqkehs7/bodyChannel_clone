import styled from '@emotion/styled';

export const DropDownMenuItem = styled.li`
  color: #fff;
  cursor: pointer;

  &:hover {
    color: #ff4d4c;
    transition: 0.5s;
  }

  a {
    padding: 10px 0px;
    font-size: 14px;
    display: block;
  }
`;
